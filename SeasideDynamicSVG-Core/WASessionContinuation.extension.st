Extension { #name : #WASessionContinuation }

{ #category : #'*SeasideDynamicSVG-Core' }
WASessionContinuation >> svgExpired [
	" this continuation has been remove from the contination store.
	hook to allow SVG continuations to unregister external SVG documents "
]
